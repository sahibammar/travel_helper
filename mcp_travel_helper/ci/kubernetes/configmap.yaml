# Optional: ConfigMap with sample data/travel_helper.json
# Use this to override the image's default empty data, or replace with your own data.
# To use: uncomment the volumeMounts and volumes in deployment.yaml, then apply this ConfigMap.
apiVersion: v1
kind: ConfigMap
metadata:
  name: travel-helper-mcp-data
  labels:
    app: travel-helper-mcp
data:
  travel_helper.json: |
    {"cheapest_flights": [], "cheapest_flights_with_hotels": []}
