# MCP Travel Helper server — Service
apiVersion: v1
kind: Service
metadata:
  name: travel-helper-mcp
  labels:
    app: travel-helper-mcp
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: travel-helper-mcp
